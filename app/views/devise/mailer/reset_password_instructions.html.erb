<% translate('devise.mailer.reset_password_instructions.body').each do |line| %>
  <p><%= line %></p>
<% end %>

<p>
  <%= link_to translate('devise.mailer.reset_password_instructions.link_label'),
              edit_password_url(@resource, reset_password_token: @token) %>
</p>

<p><%= translate('devise.mailer.reset_password_instructions.disclaimer') %></p>
